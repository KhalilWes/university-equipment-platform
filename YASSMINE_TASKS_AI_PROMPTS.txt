YASSMINE - SPRINT 2 & 3 TASKS
Project: Equipment Booking System | Date: 2026-04-02

🚨 BLOCKERS: Wait for backend endpoints
- POST /api/reservations (Mohamed Amine)
- GET /api/reservations/student/:studentId (Hadhemi)

════════════════════════════════════════════════════════════════════════════

SPRINT 2 (CRITICAL)

P1 - API Booking Integration
File: StudentCatalog.jsx
Replace handleConfirmReservation() to POST to backend instead of alert().

AI Prompt:
"In StudentCatalog.jsx, replace handleConfirmReservation() to call 
POST http://localhost:5000/api/reservations with body { equipmentId: 
selectedEquipment._id, startDate, endDate } and Bearer token. On success 
close modal and show success feedback. On error show error message. Remove 
alert(). Keep date validation."

Commit: git commit -m "feat(student): booking API integration"

────────────────────────────────────────────────────────────────────────────

P2 - MyReservations API Page
File: MyReservations.jsx, Etudiant.jsx (minor)
Fetch reservations from GET /api/reservations/student/:id instead of props.

AI Prompt:
"In MyReservations.jsx, add useState/useEffect to fetch reservations from 
GET /api/reservations/student/:studentId (from localStorage) with Bearer 
token. Show loading/error/empty states. Compute stats from API data. Stop 
relying on props. Keep layout same."

Commit: git commit -m "feat(student): API-driven reservations page"

────────────────────────────────────────────────────────────────────────────

P3 - Date Range Picker
File: StudentCatalog.jsx, package.json
Install react-day-picker, replace two date inputs with one range picker.

AI Prompt:
"Install react-day-picker in Client. In StudentCatalog.jsx booking modal, 
replace the two date <input> with react-day-picker range component. Bind 
selected range to startDate/endDate. Keep French labels and validation."

Commit: git commit -m "feat(student): date-range picker"

────────────────────────────────────────────────────────────────────────────

P4 - Status Normalization
File: StudentCatalog.jsx, MyReservations.jsx
Use pending/approved/refused internally, French labels in UI.

AI Prompt:
"Create getStatusLabel(status) helper: pending→'En attente', 
approved→'Approuvée', refused→'Refusée'. In StudentCatalog.jsx and 
MyReservations.jsx, use lowercase English status internally, use helper 
for UI display. All API calls send/receive lowercase English."

Commit: git commit -m "fix(student): normalize status values"

════════════════════════════════════════════════════════════════════════════

SPRINT 3 (POLISH)

P5 - Toast Notifications
File: StudentCatalog.jsx, MyReservations.jsx, main.jsx
Install react-hot-toast, remove alert(), show toast.success/error.

AI Prompt:
"Install react-hot-toast in Client. Add <Toaster /> in main.jsx. In 
StudentCatalog.jsx replace alert() with toast.success(). Add toast.error() 
for API/validation failures. Same in MyReservations.jsx. No other changes."

Commit: git commit -m "feat(student): toast notifications"

────────────────────────────────────────────────────────────────────────────

P6 - Loading Skeletons
File: StudentCatalog.jsx, MyReservations.jsx
Show skeleton cards/rows while loading instead of text.

AI Prompt:
"In StudentCatalog.jsx while isLoading, render 3 skeleton cards matching 
card layout (gray boxes with pulse animation). In MyReservations.jsx, 
render skeleton rows. Use CSS keyframe pulse. Keep existing CSS files."

Commit: git commit -m "feat(student): loading skeletons"

════════════════════════════════════════════════════════════════════════════

✅ DO BEFORE PR:
[ ] Booking works end-to-end (submit → backend)
[ ] MyReservations loads from API on mount
[ ] Date picker selects ranges correctly
[ ] Status labels are French in UI, English in logic
[ ] Toasts show for success/error (no alerts)
[ ] Skeletons load while fetching
[ ] No console errors
[ ] Token sent in all API calls
