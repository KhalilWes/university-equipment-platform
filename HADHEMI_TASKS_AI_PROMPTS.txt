HADHEMI - REMAINING TASKS ONLY
Project: Equipment Booking System | Updated: 2026-04-10

════════════════════════════════════════════════════════════════════════════

SPRINT 3 (PENDING)

H4 - Maintenance Logs API (InterventionLog)
Files: server/models/Equipment.js, server/routes/equipmentRoutes.js

Goal:
- Add an InterventionLog array on Equipment to track technician repairs.
- Expose API endpoints to append/read logs.

Required Data:
- technicianId (ref User)
- note/description
- statusBefore, statusAfter
- createdAt

AI Prompt:
"Update Equipment schema to add interventionLogs array with fields: technicianId (ObjectId ref User), note (String), statusBefore (String), statusAfter (String), createdAt (Date default now). Add endpoints: POST /api/equipment/:id/interventions (Technician/Admin) to add a log and optionally update status; GET /api/equipment/:id/interventions to list logs sorted desc. Return { success, data, message } with proper 400/403/404/500 handling."

Commit:
git commit -m "feat(backend): add maintenance intervention logs API"

════════════════════════════════════════════════════════════════════════════

✅ DO BEFORE PR:
[ ] Equipment schema stores intervention logs
[ ] Technician/Admin can add intervention entry
[ ] Logs endpoint returns sorted intervention history
[ ] Role protection on write endpoint
[ ] Clear error responses and no console errors
