MOHAMED AMINE - SPRINT 2 TASKS (BACKEND ENGINE)
Project: Equipment Booking System | Date: 2026-04-02

════════════════════════════════════════════════════════════════════════════

SPRINT 2 (CRITICAL - OTHERS BLOCK ON THIS)

M1 - Reservation Model
File: server/models/Reservation.js
Create MongoDB schema for reservations.

Details:
- Fields: userId (ref User), equipmentId (ref Equipment), startDate, endDate, status
- Status enum: pending, approved, refused
- Timestamps: createdAt, updatedAt
- Indexes on userId + equipmentId for fast queries
- Optional: notes field for admin comments

AI Prompt:
"Create server/models/Reservation.js with Mongoose schema. Fields: userId 
(ObjectId ref User), equipmentId (ObjectId ref Equipment), startDate (Date), 
endDate (Date), status (enum: pending/approved/refused, default pending), 
createdAt, updatedAt. Add compound index on (equipmentId, startDate, endDate) 
for availability checks. Export model as Reservation."

Commit: git commit -m "feat(backend): create Reservation model"

────────────────────────────────────────────────────────────────────────────

M2 - Check Availability Logic
File: server/routes/equipmentRoutes.js or new server/utils/availability.js
Function to check if equipment is available for date range.

Details:
- Input: equipmentId, startDate, endDate
- Check if any existing reservation overlaps dates
- Return: { available: boolean, conflictingReservation?: {...} }
- Used by POST /api/reservations before creating

AI Prompt:
"Create a checkAvailability(equipmentId, startDate, endDate) function in 
server/utils/availability.js. Query Reservation model for any pending or 
approved reservations with overlapping dates for same equipmentId. Return 
{ available: true/false }. Handle date comparison correctly (inclusive 
ranges). Export function."

Commit: git commit -m "feat(backend): add availability check logic"

────────────────────────────────────────────────────────────────────────────

M3 - POST /api/reservations Endpoint
File: server/routes/equipmentRoutes.js or server/routes/reservationRoutes.js
Create booking endpoint.

Details:
- Auth required: JWT
- Body: { equipmentId, startDate, endDate }
- Validate: dates provided, end >= start, equipment exists
- Call checkAvailability() — reject if not available
- Create Reservation doc with status pending
- Response: { success: true, data: { _id, status, ... } }

AI Prompt:
"Create POST /api/reservations endpoint in equipment routes. Extract 
userId from JWT middleware. Validate request body (equipmentId, startDate, 
endDate present and valid). Call checkAvailability(). If available, create 
Reservation doc with status pending. If not available, return 409 conflict. 
Respond with { success: true/false, data: {...}, message: '...' }. Include 
try/catch error handling."

Commit: git commit -m "feat(backend): POST /api/reservations endpoint"

════════════════════════════════════════════════════════════════════════════

✅ DO BEFORE PR:
[ ] Model creates documents correctly
[ ] Availability check detects overlaps
[ ] POST endpoint validates all inputs
[ ] POST checks availability before creating
[ ] Duplicate bookings rejected (409)
[ ] Invalid dates rejected (400)
[ ] Missing equipmentId rejected
[ ] JWT auth required
[ ] Response format is { success, data, message }
[ ] Dates stored as ISO strings or Date objects consistently
